pre-commit:
  parallel: true
  commands:
    ruff-format:
      glob: "*.py"
      run: ruff format --check {staged_files}
    ruff-lint:
      glob: "*.py"
      run: ruff check {staged_files}
    nix-check:
      glob: "*.{py,nix,toml,yml}"
      run: nix flake check --no-warn-dirty 2>&1
